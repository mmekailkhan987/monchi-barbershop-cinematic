import Home from "./pages/Home";

/**
 * Chrome & Cut direction: a dark editorial shell with brass-orange signals,
 * real neighborhood facts, and motion that behaves like a camera.
 */
export default function App() {
  return <Home />;
}
